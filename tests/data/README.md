# Test data directory

This directory is intended for test data that does not need to be installed
with the package.

It *will* be included in source distributions,
with the exception of the datasets/ subdirectory,
which is intended for submodules.
